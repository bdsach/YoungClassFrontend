export interface Classroom {
  id: number;
  name: string;
  description: string;
  category: string;
}

export interface CreateClassroom {
  name: string;
  description: string;
  category: string;
}

